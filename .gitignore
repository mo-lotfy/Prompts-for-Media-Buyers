node_modules/
.env
.env.local
.DS_Store
**/.DS_Store
*.log

# dist is generated; committed to main only via deploy, not in PRs
dist/

# scratch space for seed output and other local-only files — never committed
_staging/
